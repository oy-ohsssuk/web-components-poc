import { Meta, Source } from "@storybook/blocks";

<Meta title="utils/review-in-catalog/scrollToReview" />

## scrollToReview

```ts
scrollToReview(el: HTMLElement, reviewId: string): boolean
```

**props**

`el: HTMLElement` 루트 엘리먼트  
`reviewId: string` 리뷰의 고유 ID

**return**

`boolean` 스크롤 성공 여부

<br />
**설명:** 특정 리뷰로 스크롤 이동합니다.

---
